"""Tools. Each tool is a self-contained module that becomes a tab.

To add a tool: subclass ``ToolBase`` (see ``pitchviz.tools.base``) and register it
in ``pitchviz.shell``'s ``TOOLS`` list. To remove one, delete it from that list.
"""
